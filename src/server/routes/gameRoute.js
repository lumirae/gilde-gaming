console.log('gameRoute.js = Loaded');

const express = require('express');
const router = express.Router();
const { gameController } = require('../controllers/gameController.js');



module.exports = router;
